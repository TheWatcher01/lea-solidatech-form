# Configuration SMTP pour l'envoi d'emails
# Copie ce fichier en .env.local et remplis les valeurs

# === SMTP Configuration ===
# Pour Gmail : smtp.gmail.com (port 587)
# Pour Outlook : smtp-mail.outlook.com (port 587)
# Pour OVH : ssl0.ovh.net (port 465)
# Pour Brevo (ex-Sendinblue) : smtp-relay.brevo.com (port 587)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# Identifiants SMTP
# Pour Gmail : utilise un "Mot de passe d'application" (pas ton mot de passe principal)
# https://myaccount.google.com/apppasswords
SMTP_USER=ton-email@gmail.com
SMTP_PASS=xxxx-xxxx-xxxx-xxxx

# === Email Configuration ===
# Adresse email qui recevra les inscriptions
EMAIL_TO=info@asso-lea.org

# Adresse email de l'expéditeur (optionnel, utilise SMTP_USER par défaut)
EMAIL_FROM=noreply@asso-lea.org
